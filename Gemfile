source 'http://rubygems.org'

gem 'rails', '3.0.5'
gem 'pg', '~> 0.10.1'

# Let's build using commands rather than plain apps
gem 'komando', '~> 0.1.0'

group :development, :test do
  gem 'ruby-debug19'

  gem 'ZenTest'
  gem 'autotest-growl'
  gem 'autotest-fsevent'

  gem 'shoulda'
  gem 'rspec-rails'
  gem 'capybara', :git => 'git://github.com/jnicklas/capybara.git'
  gem 'steak'
  gem 'launchy'
  gem 'database_cleaner'

  gem 'thin'
end

# vim: ft=ruby
